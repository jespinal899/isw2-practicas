/**
 * Registra un paciente en el sistema de manera básica.
 * 
 * @param {Object} paciente - Estructura de datos del paciente a registrar
 * @returns {boolean} true indicando éxito en el registro
 */
const registrarPaciente = (paciente) => true;
